<div class="container mt-4">
  <h1>Watch List</h1>

  <% @lists.each do |list| %>
  <br><%= link_to list.name, list %>
  <% end %>
  <br>
  <%= link_to "Add new list", new_list_path, class: "btn btn-dark" %>
</div>
